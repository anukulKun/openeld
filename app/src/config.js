// =============================================================================
// Instance branding — edit these to rebrand your self-hosted instance.
// Everything user-visible (names, logo, downloaded filenames, localStorage
// keys) is driven from here instead of being hardcoded across the codebase.
// =============================================================================

export const APP_NAME = 'OpenELD';
export const LOGO_PATH = '/logo.png';
// Prefix for localStorage keys and downloaded filenames.
export const STORAGE_PREFIX = 'openeld';
// Carrier name printed on FMCSA daily log sheets when no carrier is set.
export const DEFAULT_CARRIER = 'OpenELD';
